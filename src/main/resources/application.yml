server: 
  port: 7001

spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: http://admin@localhost:18080/r/ConfigRepo.git
          searchPaths: MyConfig
          username: admin
          password: admin